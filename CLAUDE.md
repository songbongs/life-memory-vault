# CCC 봇 운영 지침

## 요청 수신 즉시 시작 알림 (필수)

텔레그램으로 메시지를 받으면 **가장 먼저 할 일은 telegram.sendMessage 호출**이다. 어떤 생각이나 도구 실행보다 앞서 반드시 먼저 보낸다.

- "lint 정리할게요, 잠시만요 ⏳"
- "enrich 처리 시작합니다 ⏳"
- "파이썬 권한 확인 중입니다 🔧"
- "확인해볼게요 ⏳"

작업이 오래 걸릴 경우 중간 상황도 짧게 알린다. 완료 후 결과를 보고한다.

## 약속한 후속 보고는 반드시 이행 (필수)

"확인해서 알려드릴게요", "몇 분 걸립니다" 라고 말했으면 **끝나는 즉시** 보고한다.
오래 걸리는 명령은 **반드시 백그라운드로 돌린다.** 전경에서 돌리다 멈추면 세션 전체가 정지해
약속한 보고가 통째로 사라진다. (2026-08-03: 실제로 2시간 50분 정지 → 보고 누락)

## Python 권한 오류 (Operation not permitted) 대응

**트리거 표현**: "python 권한", "lint 권한 오류", "python 안돼", "Operation not permitted", "mem.py 권한"

**발생 조건**: `brew upgrade python@3.14` 이후 macOS TCC 상태 고착

> ⚠️ **`bash ~/fix-python-tcc.sh` 를 전경에서 바로 실행하지 말 것.**
> 이 스크립트는 "Python 재승인 트리거..." 단계에서 **macOS 권한 허용 팝업을 기다리며 무한 정지**한다.
> 사장님이 팝업을 누르지 않으면 영원히 반환되지 않고, 세션 전체가 그 자리에서 멈춘다.
> 2026-08-03 01:12에 실제로 이 스크립트 때문에 04:02까지 세션이 멈췄다.

**순서대로 시도한다 (스크립트는 마지막 수단):**

1. **다른 방법으로 먼저 읽어본다.** 권한 제약은 도구마다 다르게 걸린다. 실제로
   2026-08-03에는 스크립트 없이 `/bin/cat`, `/usr/bin/head`, `/bin/cp` 로 그냥 읽혔다.
   ```bash
   /bin/cat <파일>          # 또는
   /usr/bin/head -50 <파일> # 또는
   /bin/cp <파일> <작업폴더>/  # 복사본을 읽기
   ```
2. 1번이 다 실패했을 때만 스크립트를 쓴다. 이때 **반드시**:
   - 먼저 텔레그램으로 알린다 — "맥에 권한 허용 팝업이 뜰 수 있습니다. 뜨면 눌러주세요 🔧"
   - **백그라운드로** 실행한다 (전경 금지)
   - 2분 안에 안 끝나면 포기하고, 멈췄다는 사실을 텔레그램으로 보고한다

수정 후 원래 요청(lint, enrich 등)을 이어서 실행하고 결과를 텔레그램으로 보고한다.
